App(
    appid="ble_inspector",
    name="[F] BLE Inspector",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="ble_inspector_app",
    requires=["gui", "furi", "bt"],
    stack_size=4 * 1024,
    fap_category="Bluetooth",
    fap_version="1.1.0",
    fap_author="Personfu",
    fap_description="BLE advertising packet decoder — raw service UUIDs, manufacturer data, TX power, flags, payloads",
)
