App(
    appid="ettercap_mitm",
    name="Ettercap MITM",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="ettercap_mitm_app",
    requires=["gui", "furi"],
    stack_size=4 * 1024,
    fap_category="Tools",
    fap_version="1.0",
    fap_author="@CYBERFLIPPER",
    fap_description="ARP spoofing & MITM analysis controller via UART to ESP32 (ettercap-inspired)",
)
