App(
    appid="channel_analyzer",
    name="Channel Analyzer",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="channel_analyzer_app",
    requires=["gui", "furi"],
    stack_size=4 * 1024,
    fap_category="Tools",
    fap_version="1.0",
    fap_author="@CYBERFLIPPER",
    fap_description="WiFi channel congestion visualizer — real-time bar chart across all 14 channels via ESP32",
)
