App(
    appid="pwnagotchi_detect",
    name="[F] Pwnagotchi Detect",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="pwnagotchi_detect_app",
    requires=["gui", "furi"],
    stack_size=4 * 1024,
    fap_category="Tools",
    fap_version="1.1.0",
    fap_author="Personfu",
    fap_description="Pwnagotchi detector & spam tool — detect nearby Pwnagotchis or flood grid with fake identities",
)
