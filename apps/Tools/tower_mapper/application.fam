App(
    appid="tower_mapper",
    name="[F] Tower Mapper",
    apptype=FlipperAppType.EXTERNAL,
    entry_point="tower_mapper_app",
    requires=["gui", "furi"],
    stack_size=4 * 1024,
    fap_category="Tools",
    fap_version="1.1.0",
    fap_author="Personfu",
    fap_description="Cellular tower scanner & mapper — logs CID/MCC/MNC/RSSI for coverage research (Tower-Hunter-inspired)",
)
